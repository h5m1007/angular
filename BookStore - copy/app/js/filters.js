define(['angular'], function(angular) {
    angular.module('bookStoreFilters', [])
        .filter('bookStoreFilter_1', ['$scope',
            function($scope) {}
        ])
        .filter('bookStoreFilter_2', ['$scope',
            function($scope) {}
        ]);
});
